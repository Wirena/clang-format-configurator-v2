

class Formatter{

    constructor(callback, config){
        this.callback=callback;
        this.config=config;
    }

    //remove options, whose values are equal to values defined by selected style(BasedOnStyle)
    removeDefaultStyleDuplicates(selectedOptions){

    }


    format(selectedOptions){

    }
}